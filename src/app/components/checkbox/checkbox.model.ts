export interface CheckboxModel {
  className: string;
  disabled: boolean;
  errorMessage: string;
  id: string;
  indeterminate: boolean;
  label: string;
  name: string;
  required: boolean;
  value: boolean;
}
