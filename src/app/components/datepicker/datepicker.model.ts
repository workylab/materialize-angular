export interface DatePickerModel {
  autocomplete: string;
  className: string;
  disabled: boolean;
  floatLabel: string;
  format: string;
  fullSize: boolean;
  id: string | null;
  hasCounter: boolean;
  name: string;
  maxLength: number;
  placeholder: string;
  required: boolean;
  type: string;
  value: Date;
}
